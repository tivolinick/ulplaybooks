---
- name: Report hostname and OS
  hosts: all
  gather_facts: true
  tasks:
    - name: Display hostname and operating system
      debug:
        msg: "Host={{ ansible_hostname }} | OS={{ ansible_distribution }} {{ ansible_distribution_version }} ({{ ansible_os_family }})"
